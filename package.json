{
    "name": "polkadex.js",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "@types/node": "^18.11.18",
        "lerna": "^6.4.0",
        "typescript": "^4.9.4"
    },
    "scripts": {
        "build": "lerna run build",
        "publish": "lerna run build && lerna publish"
    }
}
